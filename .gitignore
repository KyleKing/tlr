# Deno
node_modules/

# Environment
.env
.env.*
!.env.example

# Build output
dist/
coverage/
*.log
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/

# Local Linear data (public repo — never commit real ticket data)
web/data/
*.tlr.sqlite
*.tlr.sqlite-*

# Throwaway spikes (never committed; delete or promote when the question is answered)
/spike/

# Decks (public repo — a talk usually carries real figures; the theme and template are tracked)
/presentations/decks/
